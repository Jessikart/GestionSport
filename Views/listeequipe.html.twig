{% extends "index.html.twig" %}

{% block section %}
<table class="table table-hover table-condensed">
<thead>
<tr><th>Nom</th><th>Date de creation</th><th>Embleme</th><th>Entraineur</th><th>Favoris</th></tr>
</thead><tbody>

{% for equipes in equipes %}
    <tr><td>{{ equipes.nom }}</td><td>{{ equipes.date_de_creation }}</td><td><img src="{{ equipes.embleme }}" class="rounded ms-2" alt="Emblème de {{ equipes.nom }}" style="width: 50px; height: auto;"></td><td>{{ equipes.entraineur }}</td><td><input type="checkbox" name="favoris_{{ equipe.id }}"></td></tr>
	
{% endfor %}
</tbody></table>

{% endblock %}